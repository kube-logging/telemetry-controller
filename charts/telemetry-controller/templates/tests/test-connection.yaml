apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telemetry-controller.fullname" . }}-test-connection"
  labels:
    {{- include "telemetry-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telemetry-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
